pre-commit:
  parallel: false
  commands:
    fmt:
      run: cargo fmt --check
    clippy:
      run: cargo clippy --workspace -- -D warnings

pre-push:
  parallel: false
  commands:
    build:
      run: cargo build --workspace
    test:
      run: cargo test --workspace
