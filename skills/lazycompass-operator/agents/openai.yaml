interface:
  display_name: "LazyCompass Operator"
  short_description: "Read-only-first LazyCompass CLI/TUI operator"
  default_prompt: "Use $lazycompass-operator to run LazyCompass safely in read-only mode first, and perform writes only when explicitly approved."
