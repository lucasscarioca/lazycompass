interface:
  display_name: "LazyCompass Operator"
  short_description: "Safe LazyCompass CLI query operator"
  default_prompt: "Use $lazycompass-operator to inspect LazyCompass config and run safe read-only CLI queries first; only perform DB writes when explicitly approved."
